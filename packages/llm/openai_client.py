"""
Deprecated module retained temporarily for compatibility. Use `packages.llm.text_processor` instead.

This file intentionally contains no provider code or secrets. It is preserved
to avoid import errors in downstream code during the migration window.
"""